# Copy this file to `.env` and fill in the secrets before running the pipeline.
#
# QUALTRICS_API_TOKEN   – API key with access to the Gauteng Wellbeing Mapper survey.
# PRIVATE_KEY_PASSWORD  – Optional: password that unlocks the RSA private key used for decrypting payloads.
#                         Leave empty or delete to be prompted securely at runtime.
# PRIVATE_KEY_PATH      – Optional: absolute path to your private key PEM. If omitted, the
#                          pipeline expects `pipeline_toolkit/secrets/private_key.pem`.
# QUALTRICS_BASE_URL    – Qualtrics API host for your tenant.
# QUALTRICS_SURVEY_ID   – Survey ID of the unified Gauteng Wellbeing Mapper survey.
# BUFFERED_S3_BUCKET    – Optional: S3 bucket containing buffered payloads. Leave blank to skip.
# BUFFERED_S3_PREFIX    – Optional: override the default `buffered-surveys/` prefix inside the bucket.
# BUFFERED_S3_PROFILE   – Optional: AWS profile name if you do not want to use the default credentials.
# BUFFERED_S3_REGION    – Optional: AWS region override for the S3 client.
# BUFFERED_S3_MAX       – Optional: limit how many buffered objects to ingest (useful for tests).
# BUFFERED_S3_WRITE_RAW – Optional: set to `1`/`true` to keep individual raw payload files.
# BUFFERED_S3_WRITE_JSON – Optional: set to `1`/`true` to keep decrypted JSON when not skipping decryption.

QUALTRICS_API_TOKEN=""
QUALTRICS_BASE_URL=""
QUALTRICS_SURVEY_ID=""
#PRIVATE_KEY_PATH=""
#PRIVATE_KEY_PASSWORD=""
BUFFERED_S3_BUCKET=""
#BUFFERED_S3_PREFIX="buffered-surveys/"
#BUFFERED_S3_PROFILE=""
#BUFFERED_S3_REGION=""
#BUFFERED_S3_MAX=""
#BUFFERED_S3_WRITE_RAW="0"
#BUFFERED_S3_WRITE_JSON="0"
